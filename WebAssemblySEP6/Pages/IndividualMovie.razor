@page "/movie"

<PageTitle>Movie</PageTitle>

<style>
table {
  border-collapse: collapse;
  width: 100%;
}

.column {
  border: 1px solid #dddddd;
  text-align: left;
  padding: 8px;
}

.movie {
    vertical-align: top;
}

.list {
    padding-left: 20px;
}

</style>

<h1>Specific movie</h1>


<table>
    <tr></tr>
  <td class="movie" width="50%">
    <p>
        <img src="@movie.Image" width="300px" />
    </p>
  </td>
  <td class="movie" width="50%">
    <p>Title: @movie.Title</p>
    <p>Description: @movie.Description</p>
    <p>Directors: @movie.Directors</p>
    <p>Actors: </p>
    @if (movie.Actors != null) {
        <li>
     @foreach (var actor in movie.Actors)
        {
                <ul class="list">@movie.Actors</ul>
        }
        </li>
    }
    <table>
        <tr>
            <td class="column">
                <p>Average rating: @movie.AvgRating</p>
            </td>
            <td class="column">
                <p>Number of votes: @movie.NumberOfVotes</p>
            </td>
        </tr>
        
    </table>
        <p>Comments: </p>
        @if (movie.Comments == null)
        {
            <p>
                <em class="list">No comments</em>
            </p>
        }
        else
        {
        <li>
            @foreach (var comment in movie.Comments)
                {
                        <ul class="list">@comment.userName said: @comment.comment</ul>
                }
                </li> 
        }

        <div>
            <button class="btn btn-primary" @onclick="AddComment">Comment</button>
        </div>
    
    </td>
    
  </table>
  



